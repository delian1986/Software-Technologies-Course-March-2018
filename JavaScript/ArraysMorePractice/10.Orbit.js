function solve(input) {
    let args=input.map(Number);
    //dimensions of the matrix
    let rows=args[0];
    let cols=args[1];

    //position of the stars
    let x=args[2];
    let y=args[3];


}

solve([4, 4, 0, 0]);